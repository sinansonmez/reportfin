query Report($id: Int!) {
    report(id: $id) {
        id
        year
        quarter
        link
        downloadCount
        bankId
        bank {
            id
            name
            continent
            country
            website
        }
        createdAt
        updatedAt
    }
}